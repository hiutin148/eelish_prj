import { useState } from 'react'
import { Link } from 'react-router-dom'
import { dashboardDecks, dashboardWeek } from '../data/dashboardData'
import { flashcards } from '../../words'

export function DashboardView() {
  const [search, setSearch] = useState('')
  const [revealed, setRevealed] = useState(false)
  const [cardIndex, setCardIndex] = useState(0)
  const card = flashcards[cardIndex]
  const decks = dashboardDecks.filter((deck) => deck.title.toLowerCase().includes(search.toLowerCase()))
  return <div className="!mx-auto !max-w-7xl !space-y-8"><header className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="!mb-3 !font-mono !text-xs !uppercase !tracking-[0.18em] !text-emerald-700">Your learning dashboard</p><h1 className="!mb-3 !text-5xl !font-extrabold !leading-[0.95] !tracking-[-0.07em] !text-slate-900">Make today<br /><span className="!font-serif !font-normal !italic !text-orange-500">count.</span></h1><p className="!max-w-md !text-sm !leading-6 !text-slate-500">A focused 15 minutes is all it takes to keep your English moving forward.</p></div><div className="flex items-center gap-3 !rounded-2xl !border !border-orange-200 !bg-orange-50 !px-4 !py-3"><span className="grid size-10 place-items-center !rounded-full !bg-orange-200 !text-orange-700">✦</span><div><strong className="block !text-sm !text-slate-900">7 day streak</strong><span className="!text-xs !text-slate-500">Best: 14 days</span></div></div></header><section className="!rounded-3xl !bg-[#173f39] !p-7 !text-white !shadow-xl sm:!p-9"><p className="!mb-8 !font-mono !text-[11px] !uppercase !tracking-[0.18em] !text-emerald-200">01 · Daily practice</p><h2 className="!mb-3 !text-4xl !font-bold !leading-none !tracking-[-0.06em] !text-white">Build your<br /><span className="!font-serif !font-normal !italic !text-orange-300">vocabulary.</span></h2><p className="!mb-7 !max-w-sm !text-sm !text-emerald-100/75">12 cards are waiting for you in your active review queue.</p><Link className="inline-block !rounded-lg !bg-orange-300 !px-5 !py-3 !text-sm !font-bold !text-emerald-950" to="/practice">Start session ↗</Link></section><div className="grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><section><div className="mb-4 flex items-end justify-between"><div><p className="!mb-1 !font-mono !text-[11px] !uppercase !tracking-[0.18em] !text-emerald-700">Keep exploring</p><h2 className="!m-0 !text-2xl !font-bold !text-slate-900">Your decks</h2></div><Link className="!text-xs !font-bold !text-emerald-700" to="/decks">View all ↗</Link></div><label className="mb-3 flex items-center gap-3 !rounded-xl !border !border-slate-200 !bg-white !px-4 !py-2 !text-slate-400"><span>⌕</span><input className="!min-w-0 !flex-1 !bg-transparent !py-2 !text-sm !outline-none" aria-label="Search decks" placeholder="Search your decks" value={search} onChange={(event) => setSearch(event.target.value)} /></label><div className="grid gap-3 md:grid-cols-3">{decks.map((deck) => <article className={`!min-h-52 !rounded-2xl !p-5 !shadow-sm ${deck.color}`} key={deck.title}><span className="grid size-9 place-items-center !rounded-full !bg-white/70 !text-xs !font-bold !text-slate-800">Aa</span><h3 className="!mb-1 !mt-8 !text-sm !font-bold !text-slate-900">{deck.title}</h3><p className="!mb-5 !text-xs !text-slate-600">{deck.description}</p><div className="!mb-2 !h-1 !rounded-full !bg-white/60"><span className="block !h-full !rounded-full !bg-slate-800" style={{ width: `${deck.progress}%` }} /></div><span className="!text-[11px] !text-slate-600">{deck.progress}% complete · {deck.count} words</span></article>)}</div></section><aside className="space-y-4"><section className="!rounded-2xl !border !border-slate-200 !bg-[#fffdf8] !p-6 !shadow-sm"><span className="!font-mono !text-[10px] !tracking-[0.16em] !text-orange-600">WORD OF THE DAY</span><span className="mt-7 block !font-serif !text-sm !italic !text-slate-400">{card.type}</span><h2 className="!mb-1 !mt-1 !text-4xl !font-bold !text-slate-900">{card.term}</h2><p className="!font-mono !text-xs !text-slate-400">{card.sound}</p>{revealed ? <p className="mt-6 !text-sm !leading-6 !text-slate-700">{card.meaning}</p> : <button className="mt-6 w-full !border-t !border-slate-200 !py-4 !text-left !text-xs !font-bold !text-emerald-700" onClick={() => setRevealed(true)}>Tap to reveal meaning ↓</button>}<button className="mt-4 w-full !rounded-lg !bg-emerald-700 !py-3 !text-xs !font-bold !text-white" onClick={() => { setCardIndex((current) => (current + 1) % flashcards.length); setRevealed(false) }}>Next word</button></section><section className="!rounded-2xl !border !border-slate-200 !bg-white !p-5 !shadow-sm"><p className="!mb-1 !font-mono !text-[10px] !uppercase !tracking-[0.16em] !text-emerald-700">Your progress</p><h2 className="!m-0 !text-lg !font-bold !text-slate-900">This week</h2><div className="mt-4 flex h-28 items-end gap-2">{dashboardWeek.map((value, index) => <span className={`flex-1 !rounded-t-full ${index === 3 ? '!bg-orange-400' : '!bg-emerald-200'}`} key={index} style={{ height: `${Math.max(value * 4, 4)}px` }} />)}</div></section></aside></div></div>
}