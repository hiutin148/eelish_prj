import { useExam } from '../hooks/useExam'

export function ExamsView() {
  const exam = useExam(); const question = exam.questions[exam.index]; const time = `${String(Math.floor(exam.seconds / 60)).padStart(2, '0')}:${String(exam.seconds % 60).padStart(2, '0')}`
  return <div className="!mx-auto !max-w-6xl !space-y-7"><header className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="!mb-2 !font-mono !text-xs !uppercase !tracking-[0.18em] !text-emerald-700">Exam room</p><h1 className="!mb-2 !text-5xl !font-extrabold !tracking-[-0.07em] !text-slate-900">Practice with <span className="!font-serif !font-normal !italic !text-orange-500">purpose.</span></h1><p className="!text-sm !text-slate-500">Timed mock tests to make exam day feel familiar.</p></div>{exam.started && <span className="!rounded-xl !bg-orange-100 !px-4 !py-3 !font-mono !text-sm !font-bold !text-orange-800">{time}</span>}</header>
    {!exam.started ? <div className="grid gap-4 md:grid-cols-2">{['TOEIC quick check', 'Vocabulary sprint'].map((title) => <button className="!rounded-3xl !bg-[#173f39] !p-7 !text-left !text-white !shadow-sm hover:!shadow-lg" key={title} onClick={exam.start}><span className="!font-mono !text-xs !uppercase !tracking-[0.16em] !text-emerald-200">Mock test</span><h2 className="!mb-2 !mt-12 !text-2xl !font-bold">{title}</h2><p className="!mb-6 !text-sm !text-emerald-100/70">Listening & Reading · 20 min</p><span className="!text-xs !font-bold">30 questions ↗</span></button>)}</div> : exam.submitted ? <section className="!rounded-3xl !bg-white !p-8 !text-center !shadow-sm"><p className="!font-mono !text-xs !uppercase !tracking-[0.16em] !text-emerald-700">Test complete</p><h2 className="!my-3 !text-4xl !font-bold !text-slate-900">{exam.score} / {exam.questions.length}</h2><button className="!rounded-xl !bg-emerald-700 !px-5 !py-3 !text-sm !font-bold !text-white" onClick={exam.start}>Retake test</button></section> : <section className="!rounded-3xl !bg-white !p-6 !shadow-sm sm:!p-9"><div className="mb-8 flex items-center justify-between"><span className="!font-mono !text-xs !text-slate-400">QUESTION {String(exam.index + 1).padStart(2, '0')} / {exam.questions.length}</span><span className="!text-xs !font-bold !text-slate-400">TOEIC · Reading</span></div><div className="!rounded-2xl !bg-slate-50 !p-7"><p className="!mb-0 !text-center !text-xl !font-bold !leading-relaxed !text-slate-800">{question.prompt}</p></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{question.options.map((option) => <button className={`!rounded-xl !border !p-4 !text-left !text-sm !font-semibold ${exam.answers[exam.index] === option ? '!border-emerald-500 !bg-emerald-50 !text-emerald-800' : '!border-slate-200 !text-slate-700'}`} key={option} onClick={() => exam.setAnswers((current) => ({ ...current, [exam.index]: option }))}>{option}</button>)}</div><div className="mt-8 flex justify-end">{exam.index === exam.questions.length - 1 ? <button className="!rounded-xl !bg-orange-500 !px-5 !py-3 !text-sm !font-bold !text-white disabled:!opacity-40" disabled={!exam.answers[exam.index]} onClick={() => exam.setSubmitted(true)}>Submit test</button> : <button className="!rounded-xl !bg-emerald-700 !px-5 !py-3 !text-sm !font-bold !text-white disabled:!opacity-40" disabled={!exam.answers[exam.index]} onClick={() => exam.setIndex((current) => current + 1)}>Next question</button>}</div></section>}
  </div>
}